<%= render 'shared/navbar' %>
<h1>user dashboard</h1>
<h2>Your lessons</h2>
<%= @lesson.title %>
<%= @lesson.category %>
<%= @lesson.price %>
<%= @lesson.description %>
<%# All @lesson variables are now reachable %>
